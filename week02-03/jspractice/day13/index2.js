// const age = 23;
// const name = 'martin';
// const adult = true;
// const skills = ['js','sleeping'];
// const job = null;

// function circleArea(radius) {
//     return Math.PI * radius **2;
// }


// console.log(circleArea(5));

// const circleArea = function circleArea(radius) {
//     return Math.PI * radius **2;
// } ;

// the above is the same, on 1 we are writing a function on the 2nd one we are assigining a function to a variable.
// so... functions can be values too we can save them inside variables. we will now use them with arrow funtion =>

// We write them like this now:
// const circleArea = (radius) => {
// return Math.PI * radius **2;
// };

// DAY 13 DOM LESSON:
// const greet = () =>{
//     const input = document.querySelector('#name');
//     input.className = 'bg-red';
//     const name = input.value
//     const heading = document.querySelector('h1');
//     heading.textContent = 'Hello' + name;
//     heading.style.color = 'orange';
// };
